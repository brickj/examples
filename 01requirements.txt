# Core dependencies
pandas>=2.0.3
yfinance>=0.2.36
transformers>=4.30.0
torch>=2.0.0

# Version constraints to avoid conflicts
importlib-metadata>=3.7.0,<8.0.0

# Core packages
numpy>=1.24.3
scikit-learn>=1.3.0

# PyTorch ecosystem (compatible versions)
torchaudio>=2.6.0
torchvision>=0.16.0

# NeMo and dependencies (commented out due to numpy version conflicts)
# To install NeMo, use: pip install "numpy<1.24,>=1.22" nemo-toolkit==1.20.0 pytorch-lightning==1.9.5
# nemo-toolkit==1.20.0
# pytorch-lightning==1.9.5

# Japanese tokenization for NeMo (optional)
# ipadic
# mecab-python3

# Langchain with fixed versions
langchain==0.0.267
langchain-core==0.0.10

# Utils
tqdm>=4.65.0
huggingface-hub

# Vector DB and embeddings
sentence-transformers>=2.2.2
chromadb>=0.4.0

# MLflow and its dependencies
mlflow>=2.0.0
scikit-learn>=1.0.2
pyyaml>=5.4.1

# Additional utilities
tabulate>=0.9.0